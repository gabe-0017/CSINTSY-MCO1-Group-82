1. Open the project's root directory (the one containing freeplay.bat/sokobot.bat & freeplay.sh/sokobot.sh) in cmd/terminal.
2. Run the program with this syntax: sokobot <map_name>
   -test run on the 'testlevel' map: sokobot testlevel
